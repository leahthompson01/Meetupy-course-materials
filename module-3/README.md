# Module 3 — Modeling the Application

## Provided Files

### `DatabaseSeeder.php`

This file contains the sample Users, Meetup Groups, and Events we'll use throughout the workshop.

Copy `DatabaseSeeder.php` into your Laravel application at:

`database/seeders/DatabaseSeeder.php`

Replace the existing `DatabaseSeeder.php` when prompted.

Then seed your database:

```bash
php artisan db:seed